[project]
name = "grpc_workspace"
version = "0.1.0"
description = "gRPC workspace package for VLA Foundry project."
requires-python = ">=3.12"
dependencies = [
    "grpcio",
    "grpcio-tools",
    "numpy"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
