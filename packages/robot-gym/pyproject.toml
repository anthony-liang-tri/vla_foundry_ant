[project]
name = "robot_gym"
version = "0.1.0"
description = "Robot Gym package for VLA Foundry project."
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "drake"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
