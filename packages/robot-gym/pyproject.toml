[project]
name = "robot_gym"
version = "0.1.0"
description = "Robot Gym package for LBM2 project."
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "drake"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
