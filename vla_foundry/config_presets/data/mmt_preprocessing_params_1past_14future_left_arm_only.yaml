type: mmt_npz

past_lowdim_steps:   1
future_lowdim_steps: 14
image_indices:
- -1
- 0
stride: 1
max_padding_left: 3
max_padding_right: 15
padding_strategy: copy
filter_still_samples: False
still_threshold: 0.05
camera_names: ["rgb"]  # Depth removed, RGB only
jpeg_quality: 95
max_episodes_to_process: -1
fail_on_nan: True
skip_git_tagging: False
resize_images_size:
- 384
- 384
mmt_lowdim_flatten_indices_selection:
  # State observations - left arm only
  chassis_T_eef_pose: [{start: 0, end: 7}]    # Left arm end-effector pose in chassis frame (7-dim)
